apiVersion: v1
kind: Service
metadata:
  name: service-name
  labels:
    env: dev
    app: tomcat
spec:
  type: LoadBalancer
  selector:
    env: dev
    app: tomcat
  ports:
    - port: 8080
      targetPort: 8080
